type DataItem = {
    date: string;
    amount: number;
};
type Props = {
    data: DataItem[];
};
declare const CustomLineChart: ({ data }: Props) => import("react/jsx-runtime").JSX.Element | null;
export default CustomLineChart;
